setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
    worker: worker/Dockerfile
release:
  command:
    - ./scripts/release.sh
  image: web
run:
  web: airflow webserver -p ${PORT}
  worker: airflow scheduler